server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ManuelVillacriz/ms-course-youtube-mv.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "master"                   
  security:
    user:
      name: ManuelVillacriz
      password: Sp22051983Mv03121974*.*
